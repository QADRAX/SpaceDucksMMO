/**
 * Domain contracts for resource-persistence use cases (inputs, outputs, and shared vocabulary).
 *
 * Ports and use-case implementations live under `application/`; this module is actor-agnostic.
 */
export type * from './useCaseTypes';
export * from './catalogWire';

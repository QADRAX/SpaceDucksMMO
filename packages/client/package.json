{
  "name": "@spaceducks/client",
  "version": "0.1.0",
  "private": false,
  "main": "dist/main.js",
  "license": "MIT",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "vite build",
    "build": "npm run build:main && npm run build:renderer",
    "start": "npm run build && electron .",
    "dev": "electron .",
    "dev:main": "tsc -p tsconfig.main.json --outDir dist -w",
    "dev:renderer": "vite",
    "dev:start": "wait-on http://localhost:5173 dist/main.js dist/preload.js && electron .",
    "dev:all": "concurrently -k -n renderer,main,start -c yellow,cyan,magenta \"npm run dev:renderer\" \"npm run dev:main\" \"npm run dev:start\"",
  "dev:start:debug": "wait-on http://localhost:5173 dist/main.js dist/preload.js && cross-env NODE_OPTIONS=--inspect=9229 electron .",
  "dev:all:debug": "concurrently -k -n renderer,main,start -c yellow,cyan,magenta \"npm run dev:renderer\" \"npm run dev:main\" \"npm run dev:start:debug\"",
    "ev:dev": "electron-vite dev",
    "ev:build": "electron-vite build",
    "ev:dev:debug": "cross-env NODE_OPTIONS=--inspect=9229 electron-vite dev",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "three": "^0.181.1",
    "zod": "^3.23.8",
    "preact": "^10.19.3"
  },
  "devDependencies": {
    "electron": "^35.7.5",
    "vite": "^6.4.1",
    "concurrently": "^8.2.0",
    "wait-on": "^7.0.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.5.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/three": "^0.158.0",
    "@preact/preset-vite": "^2.9.1"
  },
  "overrides": {
    "react": "npm:preact-compat",
    "react-dom": "npm:preact-compat"
  }
}